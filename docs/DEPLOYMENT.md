# Deployment

QuorumSeal is not deployed yet. Deployment will occur only after Direct Mode, GenVM lint, schema generation, and preflight pass, followed by byte-for-byte Explorer source parity verification.
